SECRET_KEY=replace-with-a-long-random-secret
ACCESS_TOKEN_EXPIRE_MINUTES=30
DEMO_ADMIN_PASSWORD=replace-with-local-admin-password
DEMO_MEMBER_PASSWORD=replace-with-local-member-password
# Leave DATABASE_URL unset to use the default SQLite database at backend/projects.db.
# DATABASE_URL=postgresql+psycopg://username:password@localhost:5432/abb_mlops
